# Main Tables

This folder contains the canonical standalone table outputs for the main package results.

Current standalone files:

- `newspaper_source_summary.tex`: corresponds to the "Summary of Newspaper Sources" table.

`newspaper_source_summary.tex` is generated by `Code/figure_code/source_table.R` and kept here as the canonical standalone source table output.

# Main Outputs

This folder stores the canonical package outputs used in the main result set.

- `Figures/` contains the main figure files kept in the formal package.
- `Tables/` contains standalone main table outputs kept in the formal package.

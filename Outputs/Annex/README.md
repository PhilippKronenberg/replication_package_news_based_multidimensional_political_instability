# Annex Outputs

This folder stores the annex outputs that belong to the formal package.

- `Figures/` contains the annex figure files kept in the formal package.
- `Tables/` contains the annex table artifacts kept in the formal package.
